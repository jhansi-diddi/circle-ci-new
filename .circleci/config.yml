version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - echo "dev"
      - echo "Hello World"
  test:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - echo "test"
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
